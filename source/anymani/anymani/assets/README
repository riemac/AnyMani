AnyMani Assets
==============

`source/anymani/anymani/assets/` 是 AnyMani 的手资产生成子项目。

当前版本：`0.4.1`

它负责的不是单一 URDF 导出，而是一整条可追溯的资产生产链：

- `builder/`：构造 canonical `HandCfg`
- `generator/`：组织 pre-made 与 post-mutate 工作流
- `validator/`：显式拒绝机械上不合理的资产
- `exporter/`：写出 URDF / sidecar / tree / materialized meshes
- `asset_physics.py`：根据最终 collision 几何闭合 `mass / inertial`

当前稳定 contract
-------------------

1. `HandGeneratorCfg` 是最高 façade。
2. pre-made 与 post-mutate 都以 `HandCfg` 作为统一中间表示。
3. sidecar `hand.yaml` 会保存完整 `hand_cfg` 快照，供 mutate-only 恢复。
4. physics closure 在 generator 主链中、validator 之前执行。
5. `cs` procedural fingertip 在 builder / mutator 阶段保留 $r,h,\lambda=h/r$ 参数语义，但在 physics / validator / exporter 前物化为单个 watertight mesh body。
6. custom tip mesh 当前默认使用 `trimesh` 真实体积分；non-uniform mesh scale 显式 fail-hard。
7. 默认 recolor palette 为 `anatomy_soft_v1`：`*_tip` 统一 warm ivory，`*_dip` 使用紫色，以区分接触皮肤和 distal link。
8. post-mutate finger length 使用 home pose 下 collision geometry 沿 nominal distal axis 的真实轴向长度，而不是旧式 joint-origin 链长近似。
9. family composition 与 missing slots 是正交 topology 轴；`mixed=True` 只对每个 full/missing template 的存活 non-thumb 展开 LEAP / Allegro 组合，并且只有同时含两种 family 才是真正 mixed。thumb 始终与 base palm family 绑定，但不参与 mixed 判定，因为 LEAP/Allegro 的区分来自 non-thumb 运动学机制。
10. validator 前物化的 procedural mesh 是候选期产物；候选被拒绝或闭包/导出异常时，只回滚本次新写文件，不在 generated asset 树中保留 OBJ-only 半成品。
11. `summary.stats.rejected_by_reason` 按稳定 validator error-code 集合计数；多规则同时命中的样本只占一个组合键，因此原因计数总和等于 `rejected`。
12. generated left 由 canonical right 关于 palm YZ 平面做完整物理反射：位置、姿态、revolute 轴、惯量和非对称 mesh 同步 lowering，左右共享 joint identity、limits 与相同 $q$ 功能语义。
13. 新 generated bundle 在 `hand.yaml.handedness_contract` 中声明 `same_q=true` 与 `physical_lowering_complete=true`；缺少严格证书的历史 left bundle 默认拒绝加载，只能通过显式 legacy audit override 读取。
14. `link_proximal_overlap` 在 child link 局部 `+y` 轴上只改变 proximal boundary；它使用 link-scale 后的 child span 与变异前 parent 净 span 计算 signed overhang，并通过 `axial_geometry_v1` 与 `link_scale` 合成一次最终 geometry patch。
15. `bank/dataset.py` 提供通用 hand asset dataset manifest：generation run、production group、mixed composition、mother、variant set 与 partition provenance 由同一 YAML 合同解析，供 SSL/PPO/task 复用。
16. dataset schema 2.0 将 validation 与 evaluation 都拆成 `unseen_variant_set` 和 `unseen_mother`；前者只包含 train mother 的新 variants，后者以 canonical left/right mirror pair 为不可拆 holdout 单位。
17. `dataset_build` 模板使用 family composition、full/missing、missing slot、mixed composition group 与 DOF 分层选择 mother；`assets_per_lineage` 包含 mother 本体，planner 自动 lower 成实际 post-mutate variant 数。
18. 多 mother post-mutate 以完整 variant set 为进程任务；shortfall 或 geometry fingerprint 重复只 quarantine 当前 run，有限补采成功后才发布 `ssl.yaml` / `ppo.yaml`。
19. `HandGeneratorCfg.post_mutate_require_unique_geometry` 默认关闭以保留 identity 加权样本；正式 dataset recipe 开启后，mother no-op、limit-only 变化与同 variant set 重复都不计为新几何，并在当前槽位内重新抽取完整联合 proposal。

数据集构建
----------

正式构建强制先冻结并审阅 selection lock，再启动多 mother post-mutate：

```bash
python -m anymani.assets.scripts.dataset plan --template source/anymani/anymani/assets/datasets/cross_embodiment_balanced_v1/template.yaml
python -m anymani.assets.scripts.dataset build --template source/anymani/anymani/assets/datasets/cross_embodiment_balanced_v1/template.yaml --workers 8 --resume
```

`derive-ppo` 只从 completed SSL lineage pool 派生新的 PPO manifest，不生成或复制 variant assets。模板控制科研分布、资产数与随机域；mutator、validator 和 physics 参数继续由 `config/asset_gen_cfg.py::POST_MUTATE_CFG` 定义，worker 数仅是 CLI 执行参数。

近端重叠变异
------------

对第 $i$ 个 eligible child link，基础 overhang 为 $o_i^0$，link-scale 后 child span 为 $s_i^\star$，变异前 parent 净 span 为 $E_{i-1}^0$。配置采样的 signed ratio 为 $\eta_i$，parent cap 为 $\kappa$，则：

$$
o_i'=\operatorname{clip}(o_i^0+\eta_i s_i^\star,\ 0,\ \kappa E_{i-1}^0),\qquad
\delta_i=o_i'-o_i^0.
$$

最终 link geometry 为 $L_i^{final}=L_i^{scale}+\delta_i$、$c_{y,i}^{final}=c_{y,i}^{scale}-\delta_i/2$。因此 distal boundary、joint frame、joint axis、下游 joint/tip origin 和拓扑不变；`link_scale` 唯一负责 downstream origin，overlap 只负责最终 link 长度与中心。

```text
child local +y axis

before:       parent             child
              |=================|[-----------]
              ^                 ^             ^
              a                 b             distal boundary
              |<-- overhang -->|

after (+delta): parent       child
              |=============|[---------------]
              ^             ^                 ^
              a-delta       b                 same distal boundary
              |<-- final overhang -->|
```

`identity` 产生整手 no-op；`disturb` 对 eligible owner 独立采样；`homologous_non_thumb` 按 `(slot family, semantic suffix)` 在 non-thumb finger 间共享 ratio，而 thumb 仍逐 owner 独立。首个 surviving active revolute child、前置 fixed root、fixed tip 与 unsupported sphere/custom mesh 不参与变异；后者显式拒绝而不是静默跳过。

每个 post-mutate sidecar 在 `post_mutate_samples.link_proximal_overlap` 与顶层 provenance 中记录 resolved mode、homologous groups、`delta_ratio`、`source_child_span_m`、`scaled_child_span_m`、`parent_span_before_m`、`base_overhang_m`、`final_overhang_m`、`max_overhang_m`、`geometry_delta_m` 与 `max_parent_overlap_ratio`，便于从数值证据复算最终 geometry。

常看入口
--------

- 配置入口：`config/asset_gen_cfg.py`
- 最高生成入口：`generator/hand_generator.py`
- dataset template/manifest：`datasets/<template_id>/`，schema/resolver：`generator/dataset_build/` 与 `bank/dataset.py`
- 物理闭包：`asset_physics.py`
- 版本记录：`VERSION`
- 变更记录：`CHANGELOG`
- 面向 agent 的约定：`AGENTS.md`

开发提示
--------

- 若改动生成主链，同时检查 `assets/AGENTS.md` 与 `assets/generator/AGENTS.md`。
- 若改动影响生成 contract、validator、physics closure 或导出语义，请同步更新 `CHANGELOG`。
- 若改动引入新的阶段级能力，优先判断它应属于 `Made / Mutate / Validate / Export / Physics` 中哪一层。
- 默认密度与 physics closure 参数统一从 `config/asset_gen_cfg.py::ASSET_PHYSICS_CFG` 修改。
