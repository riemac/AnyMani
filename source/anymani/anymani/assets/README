AnyMani Assets
==============

`source/anymani/anymani/assets/` 是 AnyMani 的手资产生成子项目。

当前版本：`0.3.3`

它负责的不是单一 URDF 导出，而是一整条可追溯的资产生产链：

- `builder/`：构造 canonical `HandCfg`
- `generator/`：组织 pre-made 与 post-mutate 工作流
- `validator/`：显式拒绝机械上不合理的资产
- `exporter/`：写出 URDF / sidecar / tree / materialized meshes
- `asset_physics.py`：根据最终 collision 几何闭合 `mass / inertial`

当前稳定 contract
-------------------

1. `HandGeneratorCfg` 是最高 façade。
2. pre-made 与 post-mutate 都以 `HandCfg` 作为统一中间表示。
3. sidecar `hand.yaml` 会保存完整 `hand_cfg` 快照，供 mutate-only 恢复。
4. physics closure 在 generator 主链中、validator 之前执行。
5. `cs` procedural fingertip 在 builder / mutator 阶段保留 $r,h,\lambda=h/r$ 参数语义，但在 physics / validator / exporter 前物化为单个 watertight mesh body。
6. custom tip mesh 当前默认使用 `trimesh` 真实体积分；non-uniform mesh scale 显式 fail-hard。
7. 默认 recolor palette 为 `anatomy_soft_v1`：`*_tip` 统一 warm ivory，`*_dip` 使用紫色，以区分接触皮肤和 distal link。
8. post-mutate finger length 使用 home pose 下 collision geometry 沿 nominal distal axis 的真实轴向长度，而不是旧式 joint-origin 链长近似。
9. `mixed=True` 只展开 non-thumb 的 LEAP / Allegro family 组合；thumb 始终与 base palm family 绑定，因为其 mount 位姿属于 palm 的 canonical 装配语义。
10. validator 前物化的 procedural mesh 是候选期产物；候选被拒绝或闭包/导出异常时，只回滚本次新写文件，不在 generated asset 树中保留 OBJ-only 半成品。
11. `summary.stats.rejected_by_reason` 按稳定 validator error-code 集合计数；多规则同时命中的样本只占一个组合键，因此原因计数总和等于 `rejected`。
12. generated left 由 canonical right 关于 palm YZ 平面做完整物理反射：位置、姿态、revolute 轴、惯量和非对称 mesh 同步 lowering，左右共享 joint identity、limits 与相同 $q$ 功能语义。
13. 新 generated bundle 在 `hand.yaml.handedness_contract` 中声明 `same_q=true` 与 `physical_lowering_complete=true`；缺少严格证书的历史 left bundle 默认拒绝加载，只能通过显式 legacy audit override 读取。

常看入口
--------

- 配置入口：`config/asset_gen_cfg.py`
- 最高生成入口：`generator/hand_generator.py`
- 物理闭包：`asset_physics.py`
- 版本记录：`VERSION`
- 变更记录：`CHANGELOG`
- 面向 agent 的约定：`AGENTS.md`

开发提示
--------

- 若改动生成主链，同时检查 `assets/AGENTS.md` 与 `assets/generator/AGENTS.md`。
- 若改动影响生成 contract、validator、physics closure 或导出语义，请同步更新 `CHANGELOG`。
- 若改动引入新的阶段级能力，优先判断它应属于 `Made / Mutate / Validate / Export / Physics` 中哪一层。
- 默认密度与 physics closure 参数统一从 `config/asset_gen_cfg.py::ASSET_PHYSICS_CFG` 修改。
