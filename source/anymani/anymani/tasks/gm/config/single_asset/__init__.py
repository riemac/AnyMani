r"""Single generated-asset GM configuration variants.

当前子包承载单资产 MLP probe，用于快速核验 action / reset / reward /
contact basin 等 MDP 语义。Gym task id 仍由 `anymani.tasks.gm` 与
`anymani.distill.rl` 注册，本文件不在 import 时注册额外环境。
"""
