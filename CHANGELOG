0.7.1 - 2026-08-24
==================

- Geometry SSL 生命周期升级为 experiment/full-checkpoint schema 7：`pretrain` 只执行在线监督、反向更新、训练日志和 epoch-boundary full checkpoint，不再隐式运行 validation、training-morphology q-bank、final evaluation、physical audit 或 retained export。
- 新增独立 `python -m anymani.distill.ssl.validate` 与 `python -m anymani.distill.ssl.evaluate` 入口；validation 只比较显式 epoch-0 baseline 与候选 checkpoints，并在独立目录发布 `best.pt`；evaluation 只消费显式 checkpoint，只有提供 baseline 时才运行训练形态 q-bank。
- canonical 三项目标权重冻结为 density/κ/derived-field = `1 / 20 / 0.01`；训练默认每 epoch 保存完整状态，并额外发布未更新的 `epoch_000000.pt` 与指向最终 immutable epoch checkpoint 的 `last.pt` 硬链接。
- full checkpoint 以 dataset SHA 和有序 `(asset_id, content_hash)` 训练轴摘要完成轻量 resume 身份核对；完整 `physical_geometry_hash` 与 split-isolation gate 移至显式事后阶段，避免 8192-asset audit 与纯训练争用资源。schema-6 calibration artifact 与 retained artifact schema 4 保持兼容。

0.7.0 - 2026-08-24
==================

- Geometry SSL 训练合同升级为 schema 6：公开层级固定为 `epoch → mini-epoch → minibatch → microbatch`，canonical 预算为 `32 × 4 × 1`，每个 `64 assets × 8 q = 512 pairs` minibatch 独立执行一次 optimizer update。
- 新增 `max_epochs` 与 `microbatch_size`，`num_minibatches` 改为每 epoch 新批数；删除跨 minibatch 的 `gradient_accumulation_steps`，microbatch 只在完整 minibatch denominator 下累计梯度并强制保留完整资产 q-block。
- validation 与 checkpoint 改为每 8 个完整 epoch 执行；full checkpoint 记录 epoch、optimizer update、新 pair、pair use 与 teacher realization 计数，只允许从 epoch 边界恢复。schema-5 full checkpoint/calibration 严格拒绝，retained artifact schema 4 保持兼容。
- JSONL 保存逐 update 的完整预算轴，TensorBoard 分离 optimizer-update 诊断与 `new_pairs_seen` 数据效率曲线；训练、验证和系统成本不再共享含糊的裸 `step` 横轴。
- 修正 density 归约：每个资产—构型样本先对其有效 owner×query×sigma 元素取均方误差，再跨样本等权；旧实现误将样本内误差求和，使 density 随监督元素数线性放大并污染权重校准。
- RTX 5070 Ti 上完成修正后的 schema-6 全量校准：8192 项不同资产、65,536 个 pairs、128 条三项 trace 全部有限；density/κ/derived-field 均值分别为 `0.194172 / 2.54571e-4 / 0.683170`，耗时 27.03 分钟，CUDA allocated peak 1.84 GiB、RSS peak 4.06 GiB、swap 为零。

0.6.11 - 2026-08-24
===================

- Geometry SSL 正式目标收敛为 density、κ 与 derived-field 三项；删除密度构型自导数、对应高阶损失配置、公式、上下文、测试和当前源码文档，paired parity 仍只用于独立评估。
- 模型输入 $q$ 不再建立梯度图，校准与固定评估在 `no_grad` 下运行；训练使用 64-sample 普通参数图分块反向，并保持完整 2048-sample accumulation group denominator 与 `128 × 64 × 8 × 5 = 160` 次更新预算不变。
- schema 5 与 retained artifact schema 4 保持不变；三项公式身份使既有五项校准和完整训练检查点 fail-closed，encoder-only retained artifact 接口保持兼容。
- RTX 5070 Ti 正式数据 smoke 完成 5 次 2048-sample update，训练核心吞吐 331.08 samples/s、CUDA allocated peak 10.28 GiB；8192-asset 三项校准在 27.78 分钟内完成，65,536 项 trace 全部有限、swap 为零、calibration CUDA peak 1.84 GiB。

0.6.10 - 2026-08-24
===================

- 修正 Geometry SSL 首次 catalog cache miss 的常驻内存不对称：slim pickle 发布后先释放完整 sidecar/visual 解析 heap，再重载同一 slim catalog；cold 与 warm 运行由此使用相同的数据对象语义和分配布局。
- 8192-asset cold probe 在 catalog resolve 后将当前 RSS 从 7.10 GiB 降至 3.14 GiB、swap 保持 0；首个 `64 assets × 8 q` 的五项 trace 与 `v0.6.9` 正式 calibration 逐值一致。

0.6.9 - 2026-08-24
==================

- Geometry SSL padding 直接读取 typed semantics 的 JOINT/TIP 轴长度，不再对完整 11,272-asset catalog 重复构造运动学与图临时量；当前数据合同仍为 `max_joint_count=16 / max_tip_count=4`。
- source 生命周期拆为 CPU `GeometrySourceCore` 与仅由当前 8-asset device subwindow 持有的完整 `GeometrySource`；16-entry/512 MiB arena、双 worker current/next 预取和 pinned handoff 保持有界并消除重复物化。
- palm anchor inside 判定改为 Warp fast-winding 批处理，`|signed_distance| <= 1 µm` 候选仍由 CPU float64 fixed-ray 复核；sampling version 升至 v2，并记录 query、kernel、复核、分歧与耗时证据。
- calibration/evaluation 使用 32-sample 无参数二阶图 microbatch；训练保持五项 objective 公式不变，以 2-sample、完整 accumulation-group denominator 的流式 backward 限制 Sobolev/chain 高阶图峰值。
- `mini_epochs=1` calibration 逐 logical batch 消费 additive statistics，并在批间回收 trimesh/NumPy host allocator 空闲页；`runtime_resources.yaml` 将 `geometry_source_arena` 更名为 `geometry_source_core_arena`，并新增 subwindow p95、进程 RSS/swap、CUDA peak 与 lifecycle 分阶段计时。
- RTX 5070 Ti 上 `8 assets × 8 q × 1 forward` 从 60.1 s 降至 10.58 s；最终 4-minibatch smoke 的 term means/traces 与未流式版本逐值一致，CUDA allocated peak 10.19 GiB、RSS peak 3.955 GiB、swap 0。

0.6.8 - 2026-08-23
==================

- Geometry SSL 资产启动链路切换到 libyaml C loader、单次 URDF mesh/RGBA XML 解析和有序多 lineage CPU resolve；slim catalog 跨进程复用并限制在 512 MiB。
- 完整 `GeometrySource` 改为进程内 `16 assets / 512 MiB` 双上限 LRU arena，`HandContainer` 恢复为纯解析对象；训练、异常退出与进程终止均不再留下逐资产 source/container pickle。
- lazy source/Sobol、ragged-anchor mask、8-asset device subwindow、16-sample forward microbatch 和 calibration 无高阶梯度图共同限制 CPU/GPU 峰值；`runtime_resources.yaml` 记录命中、驱逐与驻留字节证据。
- 固定 validation/q-bank 改为同资产组连续遍历全部 q-block，保持每资产 Sobol 测度与尾块不变，同时复用当前 device source；calibration 每个完整 accumulation group 原子更新 `loss_calibration.partial.yaml`，成功后只保留最终 artifact。
- 修正 schema 5 catalog 访问、后台 physical audit 异常取消和 source 重建生命周期；同 seed、同软件/硬件栈的独立进程 probe 对五项 objective 均值与 trace 取得逐值一致结果。

0.6.7 - 2026-08-23
==================

- Geometry SSL 删除 phase epoch 与每资产 epoch-q 配额，改由唯一 `num_minibatches / assets_per_minibatch / q_per_asset_per_minibatch` 接口直接声明新数据预算；首个 8192-asset preset 固定为 `128 × 64 × 8`。
- 新增 `mini_epochs=5` 数据复用：每 4 个新 minibatch 只 realization 一次 q/query/teacher，再执行 5 次独立 forward/backward/update，每遍重新抽 joint-sign rewrite；预实验执行相同前向顺序但不更新参数。
- 预实验 artifact 分别记录新数据量与复用后前向量，并允许正式实验引用不同 preset 后的产物辅助人工决定五项权重；程序不自动改权重。
- 配置与 full checkpoint 升级为 schema 5，全局 minibatch cursor 取代 epoch/window/q-round cursor；固定 validation/final q-bank 使用独立评估日程，旧 schema 明确拒绝。
- SSL 运行入口改为普通 `--phase / --num_minibatches / --mini_epochs / --seed / --experiment_name` flags；Hydra 只在内部组合 Python preset，shell 不再直接操作配置树路径。

0.6.6 - 2026-08-21
==================

- Geometry SSL 根配置收为 `data / method / trainer / run`；Trainer 显式声明 calibration/pretrain epochs、validation 与 final evaluation，runtime 不再临时构造科研采样配置。
- calibration 与正式训练共用完整 train partition 和同一单 epoch q/minibatch/增强语义；预实验只流式累计五项 additive statistics，不执行参数 backward、optimizer update 或自动权重 multiplier。
- `MethodSplitSession` 封装 source、Sobol cursor、resident window、固定评估和具体 batch；通用 checkpoint 只保存 method/optimizer/trainer state，GeometrySSL namespace、frame/unit 与 retained loader 归 concrete Method。
- validation 以 density、κ、derived-field 三项初始化归一化分数选择 best；冻结后在 evaluation unseen suites 上运行固定测度、六项 ablation 与 bootstrap，并显式报告空 official-zero-shot。
- 删除 `distill/presets`、独立 `MultiAnchorEvaluation` 和 geometry-specific SSL validation/checkpoint 运行时；新增正式 fit CUDA double 与真实 generated-asset lifecycle smoke。

0.6.5 - 2026-08-21
==================

- 将多锚点 Gaussian 隐式场提升为 `distill/methods` 科学聚合根：对内耦合 representation/model/objectives，对外只给 SSL trainer 封闭 `prepare / realize / forward / reduce / evaluate / export`。
- 主损失收为 density、κ、derived-field、Sobolev 与 chain；paired 不再进入训练目标。joint-sign rewrite 改为每个 `(asset,q)` 以 0.20 概率恰好翻一个有效 JOINT 的输入增强。
- representation 只交付物理 teacher；选 `A^(k)`、`StaticGeometryEvidence` 与跨结构 padding 迁入 method batch。每资产 8 套独立 anchor bank，validation 固定 `A^(0)` 与 4/16/64 mm。
- schema 4 完整实验由 Python façade 装配；`calibrate_objectives` 前向预实验不更新参数，pretrain 按公式身份、采样语义、method/model/representation 与 code revision fail-closed 核对 artifact。
- 删除联合六项 `GeometryFieldObjective` 运行时、旧 `ssl/methods` 与分片 YAML；Hydra 只保留兼容 CLI 名称。当前 `20×256` 仍是脚手架，不是已批准的 8192-asset 正式预算。

0.6.4 - 2026-08-19
==================

- AnyMani Assets 0.5.0 新增集中式 GPU post-mutate validator：单一 Warp/PyTorch service 执行 ragged micro-batch，CPU mother worker 逐任务回收，并以 scalar GPU 路径锁定数值等价。
- dataset build state 升级为 schema 2，逐 mother 持久化 ownership、generated report 与 lock-order fingerprint acceptance；recover CLI 支持完整 run adopt、partial quarantine 和精确 rollback。
- 本版本完成中断 `cross_embodiment_balanced_v1` 失败批次的审计回退；未自动启动新的 512-mother 正式构建。

0.6.3 - 2026-08-19
==================

- `HandGeneratorCfg` 新增可选的 post-mutate variant-set 静态几何唯一性闸门；正式资产 recipe 开启后，mother no-op、limit-only 变化和同 set 重复不再计为成功，而是在当前槽位预算内重新联合采样。
- 唯一性检查在 bundle 导出前执行且不改变 mother-level 进程并行；dataset builder 继续以同一 fingerprint 拒绝跨 mother/partition 重复，并在 resume 时从实际 sidecar 重算身份。
- 分层 dataset template/planner/build 编排迁入 `assets/generator/dataset_build/` 子包，明确其作为 `HandGenerator` 上层批量生产流程的职责。

0.6.2 - 2026-08-19
==================

- Assets 新增 typed YAML 驱动的分层数据集构建流程：将 pre-made inventory 规约成不可拆左右镜像 pair，按 family composition、full/missing、mixed composition、missing slot 与 DOF 确定性选择 train/validation/evaluation cohorts。
- `HandGenerator` 新增多 mother post-mutate façade，在 mother variant-set 粒度进程并行，并支持每 source 独立 variant 数、seed、summary 与 lightweight result；单 mother 调试入口保持不变。
- hand asset dataset 升级为 schema 2.0，validation 与 evaluation 均显式区分 unseen-variant-set 和 unseen-mother；SSL 按 suite 分别聚合后等权选择 checkpoint，expanded physical manifest 升级为 schema 4.0。
- 新增可恢复 `plan -> build`、geometry fingerprint 补采、quarantine、SSL/PPO manifest 发布及纯 manifest `derive-ppo`；首份 `cross_embodiment_balanced_v1` plan 冻结 512 train mothers 和 10624 个待生成 variants，本版本不执行正式 post-mutate build。

0.6.1 - 2026-08-19
==================

- 修复 generated pre-made topology 空间遗漏 mixed+missing 组合的问题，将 family composition 与 missing slots 改为正交语义，并在新 sidecar provenance 中显式记录两个轴。
- 收紧 true-mixed 定义：存活 non-thumb 必须同时包含 LEAP 与 Allegro，thumb 只服从 base palm 装配且不参与 family 判定；退化为单一 non-thumb family 的组合不再进入 mixed production group。
- 新增 registry 计数、退化反例、左右/掌基对称、临时 bundle 目录和历史 metadata 恢复合同；本版本不生成或改写正式 generated run。

0.6.0 - 2026-08-18
==================

- 将 Geometry SSL 重构为 schema 3 的 `EmbodimentPretrainCfg`：最高层只组合 concrete `data / method / trainer / evaluation / run` roles，canonical 数值迁入 packaged Hydra YAML，并删除集中式字段 parser、旧 Python canonical facade 与 schema 1/2 兼容路径。
- 新增 trainer-owned 全资产 q-round permutation、每资产连续 Sobol coverage、真实尾 asset/q/accumulation groups 和 optimizer-boundary resume；full checkpoint 保存 sampling/optimizer/selection/RNG，validation-best 另行导出不含 reader、teacher、objective 或 optimizer 的 standalone retained artifact。
- 将多锚点 Gaussian 的 density、κ、derived field、Sobolev/JVP、chain 与 paired parity 拆成独立 objective runtimes，由 method-local lazy context 共享 forward/JVP/paired graph；固定张量合同验证拆分前后 loss 与 q/κ 梯度逐元素等价。
- 保留 `python -m anymani.distill.ssl.pretrain` 与 `logs/ssl`，同步 expanded physical manifest、fixed validation、training-morphology q-bank replay、ablation 和 schema 3 文档；资产 dataset schema 与 assets 子项目版本不变。

0.5.8 - 2026-08-18
==================

- 新增 assets 层通用 `HandAssetDataset` manifest：以 `default_run_dir + runs.<alias>` 解析多个 generation runs、production groups、mixed compositions、mother 与完整 variant sets，并向 SSL/PPO/task 交付统一 lineage provenance。
- Geometry SSL 删除旧 `GeometrySSLAssetCfg` 与自动 leaf split，改为 `asset_dataset_manifest` 单路径引用；run artifact 写入原始 dataset、resolved config/hash 与 expanded asset manifest，默认输出迁移到 `logs/ssl/`。
- canonical cross-mother dataset 固定 45 train、16 validation、16 unseen-variant-set 与 17 unseen-mother assets；早期 separate-worker probe 和旧 physical-group split 实现退出主路径。

0.5.7 - 2026-08-17
==================

- 新增 canonical v1 hand runtime：typed `HandCfg` lower 为统一 16-DOF、25-body PhysX artifact，保留真实活动链 FK/limits/geometry，并以 manifest/hash/active routing/ghost marker 形成可复现 cache contract。
- 新增五 mother `AnyMani-GM-Canonical-InHand-v0` 与 `AnyMani-GM-Canonical-Unified-PPO-v0`：单一 PhysX batch、116D structured flat observation、per-env active mask、exact ghost position/velocity lock、canonical contact/reward/reset routing。
- 新增结构化 `EmbodimentPolicy` 与本地 `anymani_masked_ppo` rl_games model/agent/player：shared JOINT action head、PALM symmetric critic、global scalar `log_std`，有效 joint log-prob/entropy/bounds contract 与 permutation/mask/backward tests。
- 接入五 mother typed geometry evidence bank：真实 anchors/home surfaces/screws/q_home/owner graph 显式重排到 canonical axes，ragged anchors 使用有效 mask，PPO minibatch 内按唯一 asset row 做可微静态前向去重。
- 修正 MultiAssetSpawner round-robin prototype 与 mask/evidence row 的环境路由一致性，并将 KL scheduler、bounds 和 regularisation reduction 按 active DOF 归一化；一轮真实 PPO update 与 checkpoint restore 已通过。
- 固定五 mother IsaacSim reset/step smoke：160 env、16 actions、25 bodies、contact/reward finite，ghost tolerance 满足 $|q|<10^{-5}$ rad、$|\dot q|<10^{-3}$ rad/s；不将该 smoke 宣称为策略学习成功。

0.5.6 - 2026-08-16
==================

- assets 新增 signed `link_proximal_overlap` post-mutate 算子：在保持 joint frame、axis、distal boundary、下游 origin 与拓扑不变的前提下，对 eligible child-link 的 proximal overhang 做受控几何变异。
- 将 `link_scale` 与 proximal overlap 收敛到 typed `axial_geometry_v1` 合成器，collision/visual/inertial origin 只写入一次最终几何，动力学质量与惯量继续由 physics closure 重算。
- 正式 post-mutate 配置加入 `identity=0.2 / disturb=0.5 / homologous_non_thumb=0.3`、`overhang_delta_ratio=(-0.1, 0.2)` 与 parent-relative overlap cap；真实 LEAP mother 的 4-sample run 完成 physics、export、provenance 与 SDF validator 验证。

0.5.5 - 2026-08-15
==================

- 将 Geometry SSL 提升为 schema 2.0.0 的声明式 façade：`CanonicalResidualFamilyCfg` 显式组合 assets、representation、model、objective、protocol、Hydra trainer 与 run，`GeometrySSLExperiment.run()` 成为唯一执行入口；schema/checkpoint 1.x 明确 fail-closed。
- 将 simulator-independent POE/FK/Jacobian、owner collision union、home/anchor realization、Warp cache 与 physical identity 从 `robots` 迁入 `distill.representations.sources`，并建立 `GeometrySourceCfg -> GeometrySource` 与 `GeometryRepresentationCfg -> GeometryRepresentation` 生命周期；robots 收缩为 Isaac spawn/articulation adapter。
- 把 Gaussian sigma measure 与 sampled κ target 分离，并将网络配置展开为多锚点前端、2-layer graph-biased encoder-only Transformer、类型化 latent heads 和 density/sensitivity readers；canonical 参数量、state-dict keys、gauge/parity、FiLM 与跨结构 output/gradient 合同保持固定。
- 新增 RTX 5070 Ti retained encoder 性能 suite：`B=4096`、20 次预热、50 次 CUDA Event 实测 median 19.79 ms、p95 20.13 ms、max 20.22 ms、peak 843.55 MiB，满足 40 ms retained 子预算；SSL-only readers 训练微批次前向另行报告，不把该结果外推为完整 PPO/env 20 Hz。
- 同步迁移 source/test 所有权，并更新 representations、models、SSL、diagnostics 与 robots 的局部边界文档和 contract 入口。正式 21-asset、20-epoch pilot、official/cross-topology 泛化与 PPO transfer 仍未执行。

0.5.4 - 2026-08-15
==================

- 将训练查询改为静态 physical anchors 与 owner triangle tables 驱动的在线 Monte-Carlo realization：workspace 使用 anchor-local 均匀体积球，owner-shell 连续采 current surface 双侧壳层，owner-adjacent 均匀覆盖全部 graph one-hop neighbors。
- 为 PALM anchors 增加 $R_a=5$ cm、$\tau_a=R_a/2$ 的截断 Gaussian 径向 proposal、确定性 farthest-point selection、算法版本和实际 realization SHA-256；manifest/resume 现在严格比较 anchor、retained home-surface、Boolean/surface backend 与 surface-query sampling provenance。
- 将 density decoder 改为逐 `(owner,query,sigma)` scalar reader：query 与 $\log(\sigma/16\,\mathrm{mm})$ 走主路径，$z^{(0)}$ 逐层 FiLM 调制；训练使用 4/16/64 mm log-space ±10% jitter，validation 固定使用 4/8/16/32/64 mm。
- 固定 validation/replay digest 现覆盖 workspace/adjacent routing 与实际 sigma；新增采样分布、刚体等价、shell/adjacent、动态 sigma、fixed-sigma q-JVP、anchor provenance 和真实 LEAP CUDA/Warp q-sensitivity 合同。
- 当前证据仍限于合同测试、真实 mother CUDA teacher 与同 topology right LEAP family 的运行准备；正式 21-asset、20-epoch pilot、PPO transfer 与 official/cross-topology 泛化尚未执行。

0.5.3 - 2026-08-13
==================

- 修复 post-mutate family 的候选采样与序列化合同，以独立联合 proposal、整候选拒绝重采和确定性审计生成正式 right LEAP mother + 20 variants。
- 将 owner geometry 明确拆为 surface-first 与 optional solid：焊接 STL 重复顶点，审计 Warp float32 collapsed faces，保留最近面 provenance，并修复多 batch Warp 输出写入非连续视图导致的错误距离。
- 按 `physical_geometry_hash` 整组隔离 generated train/validation，新增 canonical signed-screw residual encoder、物理 joint-sign paired rewrite、六项全局归一化目标与 train-only 固定梯度校准。
- 闭合有界 resident-window、每资产 Sobol q provenance、epoch coverage、runtime telemetry、scientific-config resume 与 deterministic replay；将 Hydra 入口收敛为薄 façade，并按资产、调度、目标、验证和 checkpoint 职责拆分 runtime。
- 新增 held-out morphology 分层 checkpoint selection、training-morphology independent-q replay、固定 latent ablation 与 asset/q 两级 paired bootstrap；当前证据边界仍限于同 source、right-handed、同 topology、16-DOF family，正式 20-epoch pilot 与 PPO/official 泛化尚未执行。

0.5.2 - 2026-08-11
==================

- 新增 task-free multi-anchor geometry SSL 完整路径：从版本化 hand geometry semantics、robots POE/owner union 与 Warp 在线 $d/\rho/\kappa/g$ teacher，到 retained encoder、跨结构 20-JOINT/26-owner padding、五项联合目标和真实 mother tiny-overfit。
- 新增 `python -m anymani.distill.ssl.pretrain` Hydra 入口、generated 内容哈希 split、固定 validation bank、TensorBoard/JSONL/NPZ 记录、完整 resume checkpoint 与仅 `encoder.` namespace 的 PPO/IL transfer state。
- 增加 query-only、latent-shuffle 与可选 Warp/Kaolin reference diagnostics；默认 contract suite 覆盖资产 lowering、运动学/Jacobian、owner geometry、GPU target、padding 数值/梯度等价和 checkpoint 生命周期。

0.5.1 - 2026-07-20
==================

- 补齐 official LEAP combined reward 的代码内公式与 CPU tensor contract，逐项验证七个 reward 分量、success/fall/z-spin 阈值边界、20/30 Hz `divide_by_step_dt` 语义，以及 split terms 对 combined reward 的精确重组。
- 清理 `OfficialLeapReward` 构造器中未参与运行时计算的 reward 参数缓存；主线配置系数、DirectRLEnv 单步 reward 数值与 N031 dt-ablation 语义保持不变。

0.5.0 - 2026-07-17
==================

- 新增 GM single-asset palm-supported tactile rotation 的 CurrentObs/GRU 与 History30Obs/TCN 训练路线，保持 52D actor、30x52 temporal input 与独立 152D privileged central critic。
- 将 object reset 改为随 25-level curriculum 从 0 展开到 $[-\pi,\pi]$ 的 object-body yaw 右乘，取消 roll/pitch 初态扰动，并统一 object/hand material nominal 与 endpoint 常量。
- 将 actor/critic observation 拆成可审计的 semantic MDP terms；共享 joint position 与 target 均使用 $1/\pi$ 口径，privileged velocity、pose、contact 和 actual ADR 保留物理单位。
- 增加 per-env、once-per-policy-step episode diagnostics，覆盖旋转、pose、action、contact、termination 与 actual ADR；ADR level scalar 在 reset 时固化，避免 ongoing episode 被 curriculum promotion 中途改写。
- 修复 rl_games stale episode payload 重复消费和不等 reset-batch 加权，控制台显示真实 central-value loss，并同时支持 native `sigma` 与 custom TCN `logstd` 的 action standard deviation。
- 该版本改变同一 Gym ID 下的 reset distribution 与 critic feature scale；0.4.x tactile checkpoints 仅作为冻结 baseline 回放，不得续训或与 0.5.0 runs 混用。

0.4.3 - 2026-07-12
==================

- 新增 generated `right_t4_i4_m4_r4` 的 `PolicyStepTarget` train/play Gym IDs：继承 UnitRawObs 的 96D observation 与完整 MDP，只替换 target-buffer action lifecycle。
- 新动作项在每个 policy step 的 `process_actions()` 中只执行一次 $u_{t+1}=\operatorname{clip}(u_t+a_t^{exec}/24)$，physics decimation 内的 `apply_actions()` 只幂等下发同一 target；既有四次累积 action 保留为 checkpoint-compatible 对照。
- 增加纯 tensor/config contract 与 Isaac Sim lifecycle smoke，验证单次 target recurrence、重复 apply hold、partial reset、96D observation 和语义化注册接口。
- 清理项目 AGENTS/README/GM overview 中过时的阶段描述与旋转表示教条：quaternion 可作为 Isaac Lab runtime state 或显式 policy feature，policy 表示改为按 frame、reference、符号/branch 与 Markov contract 选择。

0.4.2 - 2026-07-08
==================

- 新增 generated `right_t4_i4_m4_r4` official-ADR 的 N050/N051 observation ablations：RawRadObs 与 UnitRawObs，并保持独立 train/play Gym id。
- 将 N050/N051 actor observation 与 N030 official observation 做受控对照：N050 使用 `[q_rad, u_rad]_{t-2:t}`，N051 使用 `[q/pi, u/pi]_{t-2:t}`，其余 action/reward/ADR/command/termination/reset basin 保持 N030 主线。
- 完成 current-code full-budget N30 rerun，作为 N050/N051 的同代码版本 baseline；旧 N30 继续保留为 low-ADR high-score 与 contact-semantics 风险证据。
- 基于 TensorBoard 全曲线与人工 replay，将 N051 记录为 accepted observation anchor：后段 reward/rotation 接近或略超 N30，明显优于 N50，但 gait/contact/sim2real 仍需后续 checkpoint sweep 与接触审计。

0.4.1 - 2026-07-06
==================

- 新增 generated `right_t4_i4_m4_r4` official-ADR 的 N031/N040/N041 消融入口：NoDtReward、RawDelta 与 EMAAbsolute，并保持独立 train/play Gym id。
- 将 inhand official reward 收敛到新的 `mdp/rewards.py` 主线，保留 combined official reward 与拆分 reward terms，同时出清旧 `rewards_action.py` / `rewards_task.py`。
- 在 `gm/mdp/actions` 中新增声明式 ADR joint action：relative raw-rad delta 与 joint-limit absolute EMA 共享 official action noise / latency contract，并让 N040 迁移到 `reference="current"` 的 relative cfg。
- 新增 N041 EMAAbsolute action-law ablation：保持 N030 official obs/reward/ADR 不变，仅把 target-buffer raw-delta relative 改为 joint-limit absolute target EMA。
- 新增 N031/N040/N041 contract tests 与 N041 IsaacSim smoke，验证注册、action runtime contract、96D policy obs 与 generated scene reset/step 闭环。

0.4.0 - 2026-07-01
==================

- 新增 official LEAP raw URDF asset adapter，并将 raw URDF 关节命名对齐为 `a_0..a_15`，记录 USD/URDF 同名机械关节逐项对应事实。
- 新增 `AnyMani-LeapHand-Tactile-ADR-URDF-v0` / Play 环境，在 N010 official-aligned ADR 的 observation、action、reward、termination、ADR 与 rl_games 配置不变时，仅替换 LEAP hand asset backend。
- 新增 generated `right_t4_i4_m4_r4` official-ADR probe 环境与 contract tests，复用 N010 official 96D history observation、target-buffer action、reward/ADR/termination，并接入 generated structural collision filter。
- 为 GM LEAP-URDF probe 增加 `gm_mdp.joint_pos_limit_normalized` 与 `gm_mdp.last_action` 等官方等价 observation wrapper，保留 raw-rad / processed-action 主线语义并补纯 tensor contract tests。
- 将 inhand 家族视觉门面对齐 GM 风格 skyLight/HDR，并为 generated hand 打开 visual material restore；该改动只影响可视化，不改变训练 MDP 语义。
- 将 `Research/` 与 `tools/presets/` 从 AnyMani 代码仓库追踪中移出：科研 vault 已迁移为独立 private repo，preset YAML 作为本地标定产物保留但不再污染代码 repo 状态。

0.3.0 - 2026-06-29
==================

- 初始化 AnyMani 仓库级 `VERSION` / `CHANGELOG`，把此前仅存在于 git tag 中的版本面正式落盘到仓库根目录。
- 在 `tasks/inhand` 中恢复 LEAP USD tactile 的 a51 历史黄金语义锚点：fixed z-axis continuous command、7D quaternion `goal_pose` observation、full non-tip tactile reward 与对应 Play/debug marker 路径。
- 将 `AnyMani-LeapHand-Tactile-v0` 与 `AnyMani-LeapHand-Joint-v0` 重新语义分离：tactile 恢复为 a51 连续旋转基线，joint 保持当前 RelativeSO3 fixed-goal 对照任务，不再共享同一 command / observation 语义。
- 建立 LEAP USD tactile a51 的 first-class baseline，并完成一次前 2000 epoch 的 `fall_penalty` 对照：无显式 fall penalty 的基线在后期 `reward / cumulative_rotation / consecutive_success / fingertip_contact` 上优于加入 official-style `fall_penalty=-10` 的变体。
- 在 `Research/实验记录/inhand_tactile_a51/` 新增本地实验总览与 run card，沉淀 TensorBoard 对照、checkpoint、视频路径、人工回放观察与 URDF-only 下一步建议。

0.2.1 - 2026-03-07
==================

- 回填历史 tag `v0.2.1` 的仓库级说明：该阶段围绕 inhand 任务持续迭代 command / observation / tactile 研究路线，并新增 finger gait 文献回顾与研究规划材料。
- 该版本仍沿用“只打 tag、不落盘顶层 VERSION/CHANGELOG”的旧管理方式；当前条目为事后语义回填。

0.2.0 - 2025-10-31
==================

- 完成 environment task 大重构：移除旧 v1/v2 变体，统一环境配置组织，重构 MDP 模块（observations / rewards / commands / terminations）。
- 加入 PhysX contact 参数优化与 proprioception observation 配置，形成重构后的首个稳定 release。

0.1.0 - 2025-09-20
==================

- 建立基于 `ManagerBasedRLEnv` 的任务架构，作为 AnyMani 早期环境系统的首个语义版本锚点。
