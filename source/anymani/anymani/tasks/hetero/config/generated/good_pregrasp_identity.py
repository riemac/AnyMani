r"""MVP80 active runtime的strict-v5 good-pregrasp exact identity。

正式task只从本模块读取active catalog/key协议。宽松v4身份已冻结在``good_pregrasp_identity_v4.py``，
旧artifact仍可复现，但不会被当前reset provider命中或解释成strict结果。
"""

from __future__ import annotations

import json
from typing import Any

from .strict_good_pregrasp_identity import (
    STRICT_GOOD_PREGRASP_CATALOG_ROOT,
    STRICT_GOOD_PREGRASP_GENERATION_DIGEST,
    STRICT_GOOD_PREGRASP_GENERATION_IDENTITY,
    STRICT_GOOD_PREGRASP_OBJECT_SCALE,
    STRICT_GOOD_PREGRASP_PHYSICS_DIGEST,
    STRICT_GOOD_PREGRASP_PHYSICS_IDENTITY,
    STRICT_GOOD_PREGRASP_PHYSICS_TOP_K,
    STRICT_GOOD_PREGRASP_REQUIRE_STRICT,
    STRICT_GOOD_PREGRASP_SEED,
    STRICT_GOOD_PREGRASP_SOBOL_CANDIDATES,
)

GOOD_PREGRASP_OBJECT_SCALE = STRICT_GOOD_PREGRASP_OBJECT_SCALE
GOOD_PREGRASP_SEED = STRICT_GOOD_PREGRASP_SEED
GOOD_PREGRASP_CANDIDATE_COUNT = STRICT_GOOD_PREGRASP_SOBOL_CANDIDATES
GOOD_PREGRASP_CANDIDATES_PER_BATCH = STRICT_GOOD_PREGRASP_PHYSICS_TOP_K
GOOD_PREGRASP_CATALOG_ROOT = STRICT_GOOD_PREGRASP_CATALOG_ROOT
GOOD_PREGRASP_REQUIRE_STRICT = STRICT_GOOD_PREGRASP_REQUIRE_STRICT
GOOD_PREGRASP_GENERATION_IDENTITY = STRICT_GOOD_PREGRASP_GENERATION_IDENTITY
GOOD_PREGRASP_GENERATION_DIGEST = STRICT_GOOD_PREGRASP_GENERATION_DIGEST
GOOD_PREGRASP_PHYSICS_IDENTITY = STRICT_GOOD_PREGRASP_PHYSICS_IDENTITY
GOOD_PREGRASP_PHYSICS_DIGEST = STRICT_GOOD_PREGRASP_PHYSICS_DIGEST


def good_pregrasp_generation_identity() -> dict[str, Any]:
    r"""返回JSON深拷贝的active strict-v5 generation协议。"""

    return json.loads(json.dumps(GOOD_PREGRASP_GENERATION_IDENTITY))


__all__ = [
    "GOOD_PREGRASP_CANDIDATE_COUNT",
    "GOOD_PREGRASP_CANDIDATES_PER_BATCH",
    "GOOD_PREGRASP_CATALOG_ROOT",
    "GOOD_PREGRASP_GENERATION_DIGEST",
    "GOOD_PREGRASP_GENERATION_IDENTITY",
    "GOOD_PREGRASP_OBJECT_SCALE",
    "GOOD_PREGRASP_PHYSICS_DIGEST",
    "GOOD_PREGRASP_PHYSICS_IDENTITY",
    "GOOD_PREGRASP_REQUIRE_STRICT",
    "GOOD_PREGRASP_SEED",
    "good_pregrasp_generation_identity",
]
