0.3.0 - 2026-06-16
==================

- 新增 hand asset bank façade，提供 `HandBankCfg` / `HandBank` / `HandSelection` 与虚拟标准 bundle 视图，统一 post-mutate hand asset 的路径解析、mesh relpath 校验、sidecar 读取和 visual RGBA 解析。
- 支持 `HandBankCfg.containers=("sample_id", ...)` 简写，并将显式 / 全量 / 固定 seed 采样选择结果暴露给 `tasks/gm` 与后续 `distill` 配置消费。
- 将 GM heterogeneous smoke 迁移到 `HandSpawnCfg + HandSpawnAdapter`，验证 asset bank 可直接 lower 成 IsaacLab `ArticulationCfg`，并保留 round-robin same-schema hand routing。
- 删除旧 `tasks/gm/asset_binding.py` 单资产绑定路线，后续 GM / distill 侧统一通过 asset bank 与 hand spawn adapter 表达 generated hand 选择与装配。

0.2.1 - 2026-06-10
==================

- 出清旧高饱和 `anatomy_v1` palette，`recolored=True` 与默认命名入口统一收敛到 `anatomy_soft_v1`。
- 调整 `anatomy_soft_v1` 的末端视觉语义：`*_tip` 统一为 warm ivory，`*_dip` 改用原 tip 紫色，避免 distal link 与第二段深青/靛蓝视觉重复。
- 更新 recolor / pre-made / post-mutate 回归测试，锁住 named palette 导出、显式 link override、以及 custom tip / procedural `cs` 共享 tip 颜色 contract。

0.2.0 - 2026-06-09
==================

- 将 procedural `cs` fingertip 的导出 contract 从 `cylinder + sphere` 两个 collision / visual slot 改为单个 watertight mesh body，保持 $r,h,\lambda=h/r$ 参数语义不变。
- 新增 `procedural_meshes.py`，在 generator physics closure 前物化 `cs` OBJ，并兼容历史 sidecar 中的 two-primitive `cs` schema。
- `UrdfWriter` 增加 direct-export 兜底物化，防止 `procedural://anymani/cs_tip?...` 中间 URI 泄漏进 URDF。
- `asset_physics.py` 对 single-mesh `cs` 使用 `trimesh` 质量属性后端，但密度通道仍归入 `fingertip`，不误用 `custom_tip`。
- 更新 `tip_replace`，使 `cs_ratio` 采样可从新 metadata / procedural URI / legacy primitive schema 中恢复当前 $r$ 与 $\lambda$。

0.1.1 - 2026-06-09
==================

- 修复 `link_scale -> tip_replace` 组合 post-mutate 中 fixed tip joint origin 被回滚的问题：`tip_replace` 现在保留 apply-time kinematic shell，只替换末端 contact embodiment。
- 新增组合回归测试，锁住末节 link 长度缩放后 $y_{tip}' = y_{tip} + (L_i' - L_i)$ 不会被 custom mesh tip replacement 覆盖。

0.1 - 2026-05-24
================

- 新增 `asset_physics.py`，将最终 collision 几何的 `mass / inertial` 闭包从 builder / mutator 中独立出来。
- `HandGeneratorCfg` 新增 `Physics` 入口，pre-made 与 post-mutate 主链都会在 validator 之前执行 physics closure。
- custom tip mesh 默认改为使用 `trimesh` 真实体积分计算质量、质心与惯量；non-uniform mesh scale 当前显式 fail-hard。
- custom mesh tip builder 只负责几何 lowering；custom tip 的最终 `mass / inertial` 全部统一交给 physics closure。
- `link_scale` 不再在 mutator 内部重建质量与惯量，相关动力学闭包统一收口到 `asset_physics.py`。
- `asset_gen_cfg.py` 新增统一的 `ASSET_PHYSICS_CFG`，pre-made 与 post-mutate 默认共享同一套密度与 physics closure 配置。
- `RecipeLoader` 支持 `Physics` 配置块的 YAML ↔ typed cfg 解析。
- 补充 assets 子项目 `README`，并刷新 `assets/AGENTS.md` / `assets/generator/AGENTS.md` 以反映新的 physics closure 与版本维护约定。
- post-mutate validator 新增 finger axial length hard gate：使用 home pose 下 collision geometry union 沿 nominal distal axis 的投影宽度约束过长手指。
- custom tip mesh 的 finger length 现在直接用 `trimesh` 顶点投影测量，不再复用旧的 joint-origin 链长近似。
- sidecar `hand.yaml.fingers[].total_length_cm` 改为记录真实轴向几何长度，与 validator 共享同一套长度定义。
